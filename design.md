
# 「拾贝」产品需求总结 (PRD Summary)

## 1. 品牌定义与定位
**拾贝 (Shi Bei)** 是一款网页内容保存工具。

*   **定位**：把网页中碎片知识用更好的文件格式保存下来。

## 2. 核心概念
*   **暂存篮 (The Basket)**：跨页面暂存采集到的内容片段。
*   **智能拼接**：把碎片内容按顺序合成为一份完整文档。
*   **AI 工具**：用于摘要、打标等增值处理。

## 3. 目标用户
*   **知识管理控**：习惯将高质量网页内容收藏至 Notion、Obsidian 的深度用户。
*   **科研/报告撰写者**：需要将网页资料转化为高保真 PDF 或 Markdown 的专业人士。
*   **开发者/博主**：需要清洗网页干扰，提取正文内容进行二次加工的用户。

## 4. 核心需求描述


### 3.1 客户端（浏览器插件）需求
*   **智能正文提取**：利用算法（如 Readability.js）自动识别并提取网页的标题、正文、作者、发布时间，剔除广告、导航栏、侧边栏等无关元素。
*   **知识暂存篮 (Knowledge Basket)**：支持跨页面采集多个内容片段，用户可以在插件 UI 中对暂存的片段进行排序、删除。
*   **智能拼接 (Smart Stitching)**：将暂存篮中的多个片段合成为一个完整文档，并自动注入溯源信息（来源标题、URL、采集时间）。
*   **图像资源处理**：
    *   **本地化转换**：为了防止防盗链失效及离线访问，插件需将页面中的所有图片即时转换为 **Base64** 编码，嵌入 HTML 结构中。支持处理懒加载图片（如 `data-src` 属性）。
    *   **性能优化**：在转换前对超大图进行比例压缩，确保数据包体积适合传输。
*   **样式重塑（Re-styling）**：
    *   **弃用原样式**：彻底抛弃原网站杂乱的 CSS。
    *   **内置模板选择**：提供多套内置的高审美 CSS 模板（如：学术、极简、暗黑模式），用户可在导出 PDF 时预览切换。
*   **高级 Markdown 引擎**：支持 GFM (GitHub Flavored Markdown)、YAML Front Matter、视频/Iframe 转换等特性。
*   **数据封包传输**：将处理过的“纯净 HTML + 选定 CSS + Base64 图片 + 元数据”打包，发送至后端。

### 3.2 后端（云函数 / Serverless）需求
*   **无状态处理**：后端无需部署长期服务器，不主动爬取网页，仅作为“转换引擎”接收插件发送的 JSON 数据。
*   **高保真文档转换**：
    *   **PDF 引擎**：调用无头浏览器（Puppeteer-core）将 HTML 按选定样式渲染并生成带有目录标签的 PDF。
    *   **Markdown/Docx 转换**：实现 HTML 到标准 Markdown 代码块及 Office 文档的逻辑映射。
*   **弹性伸缩**：利用云函数特性，处理瞬时的高并发文档转换请求，按需计费。

### 3.3 分发与集成需求
*   **多端分发适配**：
    *   **Notion**: 通过 Notion API 将内容写入指定的 Page 或 Database。
    *   **本地下载**：支持直接将生成的 PDF/Docx 推回至浏览器下载。优化了 PDF 下载稳定性，防止因 Popup 关闭导致的 Blob URL 失效。

## 4. 关键技术特性（核心优势）
1.  **隐私与合规**：不涉及用户登录态同步，不存储用户登录凭证，所有操作基于用户当前已打开的可见页面，安全无害。
2.  **内容永续性**：由于采用了 **Base64** 图片内联，生成的文档不依赖原始服务器的图片链接，彻底解决“图片挂了”的困扰。
3.  **极速渲染**：忽略原网页复杂的 JS 和冗余资产加载，只渲染核心 HTML，转换效率大幅提升。
4.  **成本最优**：采用 Serverless 架构，无固定服务器开销，适合个人或初创小团队快速上线。

## 5. 项目成功指标 (KPI)
*   **解析准确率**：主流主流媒体/博客网站的正文提取准确率 > 95%。
*   **响应速度**：从点击“分发”到获得结果（或上传成功），对于平均篇幅文章应在 5 秒内完成。
*   **跨端一致性**：导出的 PDF 文档在不同设备上的视觉排版与插件预览高度一致。

---

## 一、 项目总体目标
1.  **架构极简化**：插件端负责感知和数据采集，云服务负责高能耗计算，无中间昂贵的服务器运维。
2.  **样式标准化**：完全重写内容样式，通过内置精美主题（CSS 模板）重新渲染网页，彻底解决格式崩坏问题。
3.  **零依赖抓取**：由插件在用户当前页面完成所有数据提取（包括图片 Base64 转换），服务器不直接访问目标网站，规避反爬风险。
4.  **弹性弹性伸缩**：文件转换（PDF/Docx）全面采用云函数（Serverless），按需调用，成本极低。

---

## 二、 核心架构流程 (The Workflow)

1.  **[插件端] 提取与清洗**：`Readability.js` 提取正文，去除所有原始 Class 和 Style。
2.  **[插件端] 素材聚合**：支持将内容片段加入**“暂存篮 (Basket)”**，通过 `chrome.action.setBadgeText` 在插件图标上实时显示暂存数量。
3.  **[插件端] 图像转换**：扫描所有图片（含懒加载图），在 Canvas 中绘制并转为 **Base64** 字符串，内联到 HTML 中。
4.  **[插件端] 智能合成**：用户在暂存篮中完成排序后，执行“智能拼接”，系统自动生成带 YAML 元数据的完整 Markdown 或 HTML。
5.  **[传输层] 数据封包**：将“拼接后的 HTML + 选定 CSS + Base64 图片”封装为 JSON 传输。
6.  **[云函数端] 异步转换**：使用 Puppeteer 生成高保真 PDF。
7.  **[分发层] 结果流转**：通过浏览器直接下载方式分发结果。

---

## 三、 技术执行方案

### 1. 插件端：高效资源预处理 (Frontend)
*   **图片转 Base64 算法**：
    利用 `Image.onload` 结合 `Canvas.toDataURL()` 将图片转化为 Base64。
    *优势：* 解决图片防盗链问题；解决后端渲染时无法访问受限图片的问题。
*   **CSS 主题库**：
    自建标准库，例如：
    *   `style-modern.css`：现代感、大行距。
    *   `style-serif.css`：经典宋体/衬线体，适合长文本。
*   **Payload 优化**：
    由于 Base64 会增大体积，插件端会先对 HTML 进行压缩（Minify），确保即使带有图片，单个 JSON 封包也能在云函数的 Payload 限制（通常 6MB-10MB）之内。

### 2. 云函数端：无状态转换逻辑 (Backend - Serverless)
*   **技术栈**：Node.js + `Puppeteer-core` + `chrome-aws-lambda`。
*   **PDF 生成逻辑**：
    云函数接收到 HTML 后，将其注入一个空白 Chrome 实例。由于 HTML 已经自带了 Base64 图片和所有 CSS 样式，Puppeteer 只需要执行 `page.pdf()` 操作，耗时极短。
*   **Markdown/Docx 转换**：
    *   Markdown 使用 `Turndown`，直接在内存中完成，无需 Headless 浏览器。
    *   Docx 使用 `docx.js`，基于标准 Schema 生成。

### 3. 分发层：多平台集成
*   **中间件设计**：云函数在转换完成后，根据 `target` 参数调用不同的 Adapter（Notion Adapter, Obsidian Adapter 等）。

---

## 四、 品牌视觉与语气 (Identity)
*   **Slogan**: *“拾贝：把网页中碎片知识用更好的文件格式保存下来。”*
*   **语气 (Tone of Voice)**：简洁、直接，不拟人化。
    *   **采集成功**：“已加入暂存”
    *   **智能合成**：“合成完成”
    *   **空状态**：“暂存为空”

## 五、 核心优势分析


| 维度 | 旧方案 | **拾贝 方案** |
| :--- | :--- | :--- |
| **样式稳定性** | 容易受原网站干扰，导出变形 | **百分之百可控**，内置 CSS 保证美观 |
| **图片显示** | 经常因为防盗链导致图片碎裂 | **Base64 内联**，支持懒加载修复 |
| **内容组织** | 单次采集，碎片化严重 | **暂存篮 + 智能拼接**，聚合多源信息 |
| **服务器成本** | 需要 24/7 维护高性能服务器 | **云函数按次计费**，无调用不花钱 |

---

## 五、 技术细节补遗
1. **安全检测**：插件会自动检测受限页面（如 `chrome://`），并给予友好提示，防止注入脚本失败。
2. **持久化存储**：使用 `chrome.storage.local` 持久化暂存篮内容，确保跨标签页同步。
3. **Markdown 插件**：集成 `turndown-plugin-gfm` 以支持任务列表、表格、删除线等高级 Markdown 语法。


---

## 五、 项目执行里程碑 (Milestones)

### 第一阶段：插件端核心逻辑 (Week 1-2)
*   集成 `Readability.js`。
*   编写图片批量转 Base64 的异步工具函数。
*   设计 3 套基础 CSS 渲染模板。

### 第二阶段：云函数环境搭建 (Week 3)
*   在 AWS Lambda 或 阿里云 FC 上部署 `chromium` 环境。
*   编写 HTML to PDF 的核心转换 Function。
*   调通 JSON 传输协议。

### 第三阶段：样式调优与平台对接 (Week 4-5)
*   对比 PDF 导出效果，调整字体嵌入方案。
*   集成 Notion API，实现 PDF 直接上传至页面 Block。

---

## 六、 技术小贴士：
*   **性能提醒**：如果一个网页有 50 张超高清大图，Base64 会让体积剧增。建议在插件端做一次**图像预缩放（Resize）**，将其宽度限制在 1200px 左右再转 Base64，这样既能保证 PDF 画质，又能确保传输速度。
*   **字体处理**：在云函数生成 PDF 时，建议在 CSS 中引入 Google Fonts 的在线链接，或者将常用字体安装在云函数 Layer 中，以确保生成出来的文档不是模糊的系统默认字体。



# 产品功能分级体系

## 免费功能

- 免登录
- 只能在浏览器端导出markdown, 支持GFM等格式
- 可以粘贴到粘贴板
- 有广告

## 付费功能 （专业版）
- 需登录
- **智能拼接**：支持跨页面素材采集与合成。
- 可以导出markdown，支持 GFM 增强语法、YAML Front Matter，支持 OCR 识别公式、表格等。
- 可以导出 pdf (支持自定义主题)
- 可以导出 word
- 可以预览内容


## 付费功能 （大师版）
- 包含专业版的付费功能
- 包含 AI 摘要、AI 自动打标、团队共享采集库。
- 包含云存储
- AI 摘要、AI 自动打标、团队共享采集库。

难且有用的功能点
- **智能拼接 (已实现)**：跨页面手动控制采集，程序自动拼接和去重。
- **预览 (已实现)**
- **识别公式/表格/OCR**
- 虚拟滚动/列表
- 云存储
- 和内容创作衔接
